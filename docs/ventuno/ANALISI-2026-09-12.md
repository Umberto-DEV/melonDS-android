# melonDS Android — analisi profonda delle PR #1672 (app) e lib#16 (motore)
**12 settembre 2026.** Orchestrazione: 15 agenti (Opus per il ragionamento, Sonnet per la precisione), sola lettura sul repo,
verifica adversariale indipendente sui 12 rilievi principali (12/12 confermati sul codice). Rapporti di dettaglio nello
scratchpad di sessione (`report-*.md`, `mappa-architettura.md` 1353 righe, `verifica-adversariale.md`).
Contesto GitHub letto: le 3 PR sono OPEN, zero review. Il manutentore (11/09 15:52) chiede **una PR per fix, testata a fondo**.
Upstream (app e lib) non ha commit nuovi dopo le basi 3f3a1c82 / 431ab4bd.

## 0. Verdetto in tre righe
- **Motore (lib#16, 5 commit): solido.** Porting SCFG byte-identico all'upstream, 12/12 call site; wifi verbatim; guardia Compute
  completa; edge list rimossa senza consumatori residui; JIT chiude un bug reale. Nessun bug introdotto. Da correggere solo il
  testo (avvertenza microfono/SD invertita) e da dichiarare una seconda modifica JIT nascosta.
- **App (#1672, 21 commit): eterogenea.** 6 correzioni vere e ben fatte (multi-touch, crash <11, retry salvataggi, 2 audio,
  4 filtri su 6). Ma **2 regressioni introdotte** (filtro LCD e Scanlines a risoluzione ≥2x), **il corpo della PR afferma
  3 cose false** (errore salvataggio "segnalato", "display scale", "testato a schermo" per punti che non producono effetto
  osservabile), l'assistente BIOS ha 5 bug (ANR garantito con nand.bin), e la funzione RTC è in contrasto diretto con
  l'indicazione data ieri dal manutentore su #1664.
- **Nessun test della PR gira in automatico**: i 2 test C++ non sono compilati da nulla, il commit 97406445 ha tolto lint+unit
  test dalla CI. Sul Mac ora tutto è ricostruito: 164 test Kotlin passano, 4 APK debug compilati.

## 1. Rilievi per gravità (tutti verificati su file:riga)

### BUG introdotti dalla PR app
| # | Dove | Cosa | Prova |
|---|---|---|---|
| A1 | ShaderProgramSource.kt:89 (LCD), :133-142 (Scanlines) | `omega` ora moltiplicato per lo scale: a 4x il filtro LCD disegna 1024 celle su ~700 px → moiré. Prima era corretto (256 celle = pixel DS). Scanlines idem (192·s righe). | master usava costanti 256/386; verificato |
| A2 | MultiButtonInputLogic.kt:58-67 + DpadInputHandler | Con due dita la croce può produrre SU+GIÙ / SINISTRA+DESTRA; il core non filtra (NDS.cpp:1206-1213). Stato impossibile sull'hardware. | geometria verificata |
| A3 | MultiButtonInputHandler.kt:38,49; MultiButtonInputLogic.kt:59,62 | Allocazioni per evento (data class + LinkedHashSet) su percorso che prima non allocava, sotto il commento "avoid memory allocations"; aggravate da requestUnbufferedDispatch (per finestra, non per view). | |

### BUG preesistenti lasciati accanto al fix (lo stesso difetto corretto a metà)
| # | Dove | Cosa |
|---|---|---|
| B1 | TouchscreenInputHandler.kt:16-29 | Nessun ACTION_CANCEL: gesto revocato (tendina, back gesture, multi-window) → stilo DS premuto per sempre (onScreenRelease unico chiamante). |
| B2 | SingleButtonInputHandler.kt:10-19 | Nessun ACTION_CANCEL su 14 pulsanti (L, R, Start, Select, …). Il commento MultiButtonInputHandler.kt:53-57 "the branch every earlier report was missing" è falso per 14/16 pulsanti. |
| B3 | RuntimeLayoutView.kt:81-122 | updateInputs() sostituisce gli handler senza rilasciare i tasti premuti. |
| B4 | SaveManager.cpp:54-58 vs :82 | `Thread` non inizializzato se path vuoto, `Thread_Free(Thread)` incondizionato; il commit ha riscritto il distruttore senza toccarlo. Raggiungibile con firmware path vuoto (MelonInstance.cpp:66-74). |
| B5 | PlatformAndroid.cpp:141 "wb", UriFileHandler.cpp:57-77 "wt" | Scrittura SRAM troncante non atomica: il retry nuovo salva solo se il processo sopravvive; con guasto persistente ri-tronca ogni 1,6 s. |

### Assistente BIOS (7e946a90) — BUG propri
| # | Dove | Cosa |
|---|---|---|
| C1 | CustomFirmwarePreferencesFragment.kt:112 | `run()` sincrono nella callback del picker: copia byte per byte anche nand.bin (240 MB) sul thread UI → ANR garantito. |
| C2 | BiosGuidedFolderSetup.kt:199; verificatore :95-98 | Copia interrotta → nand.bin troncata dichiarata presente e valida per sempre. |
| C3 | BiosGuidedFolderSetup.kt:212-226 | `?: return false` senza `dest.delete()` → file 0 byte che blocca lo slot; esito riportato MISSING (falso). |
| C4 | BiosDirectoryPickerPreference.kt:40-46 | `super.onDirectoryPicked` → takePersistableUriPermission su URI figlio → SecurityException → validateDirectory mai raggiunta: icona/popup restano allo stato vecchio. |
| C5 | BiosGuidedFolderSetup.kt:168-172 | `count { it === hinted } == 1` è sempre 1: il controllo di ambiguità DSi bios7/bios9 è morto. |
| C6-C11 | rischi | ripunta bios_dir su cartella non vuota (:124-125); un .sav da 256/512 KB diventa firmware.bin; firmware DS 128 KB verde ma non bootable (SPI_Firmware.cpp:336-341); eccezioni non-IOException sul thread UI; findFile case-sensitive → "DS (1)"; assistente grigio senza use_custom_bios mentre DSi richiede entrambe le cartelle comunque. |
Zero test su BiosGuidedFolderSetup (266 righe). Stringhe pulite, dimensioni/offset 0x1D corretti contro il core.

### RTC (4 commit) — da ritirare dalla PR
- Il manutentore ha scritto ieri su #1664: sync in `MelonDSAndroid::resume()`, non nel frontend, rispettando la preferenza;
  dubita del trigger lid. Il nostro polling per frame è un terzo approccio, in conflitto testuale su 8 file con #1664.
- B: premessa "avanti è sempre sicuro" (RtcSync.cpp:12-17) non dimostrata e dubbia per Pokémon Gen4; il salto salta
  minute-carry IRQ e allarmi (RTC.cpp:298-299, 492-493); il fallback mktime==-1 (MelonInstance.cpp:756-762) stampa
  l'ora anche indietro; loadState/reset/rewind (:584-587, :275, :612) tornano indietro comunque → dopo fast-forward
  l'opzione muore (NONE per sempre) e un savestate riporta indietro.
- R: soglia 1 s (RtcSync.h:41) produce ristampe spurie; DST indietro blocca l'opzione per sempre; mktime per frame
  sul thread emu; il limitatore a 60 fps esatti (MelonDSAndroidJNI.cpp:666) fa correre l'orologio DS +0,29 %.
- Il test RtcSyncTest.cpp non è compilato da nulla e valida una soglia (3) diversa da quella spedita (1).
- **La preferenza accesa sul Thor**: nessun percorso nel diff la accende; candidati esterni: SettingsBackupManager.restore
  (solo denylist), allowBackup=true senza regole (AndroidManifest.xml:32), tocco accidentale (sotto "Enable JIT").

### Corpo della PR app: affermazioni da correggere
1. Punto 3 "the failure is reported": solo `__android_log_print`; `GetConsecutiveFlushFailures/HasPendingFlushError`
   (SaveManager.h:53-54) hanno zero chiamanti; `stageSaves()` è identica al blocco già eseguito a ogni frame (MelonInstance.cpp:392-398)
   → "seen fixed on the device" non può essere vero. Riscrivere: "retried instead of being marked as written, and logged".
2. Punto 6 "the display scale is passed": è la risoluzione interna (dimensione texture), non il display.
3. Commit 598b0c32 dichiara "Not verified on a real device".
4. Riferimenti a documenti interni inesistenti (MELONDS-DOCS/*, MELONDS-INTEGRA/*, ABXY-CORREZIONE.md…) in 12 file;
   14/21 commit con trailer Claude-Session; 7 messaggi "patch N/M: MELONDS-XXX"; autore `MELONDS-INTEGRA <melonds-integra@local>`;
   commenti italiani in MelonDS.h:73, MelonInstance.h:44-51, MelonDSAndroidJNI.cpp:471-474; nota "mutanti" in 2 test C++.

### Motore lib#16 — dettagli
- SCFG: RISCHIO in modalità DSi vera, bit 18 SCFG_EXT[1] → 0 disattiva SD/MMC (NAND+slot) per ARM7 (DSi.cpp:677): DSiWare
  direct-booted potrebbe perdere i save (reversibile dal guest, non testato). RISCHIO in DS mode: bit 13 → 0 cambia i timing
  VRAM (UpdateVRAMTimings :1296-1308). Avvertenza PR **invertita**: il microfono si perde solo in DS mode, l'SD anche in DSi.
  Savestate vecchi conservano il comportamento pre-fix fino al reset. Citare melonDS#1548 (chiusa 8/03/2026).
- JIT 3ad4e940: contiene una **seconda modifica non dichiarata** in InvalidateByAddr (ARMJIT.cpp:943-963): la `mask` interna
  ombreggiava quella esterna (`x & x` sempre vero) → prima iper-invalidazione, ora invalidazione precisa a 16 B. Bug identico
  upstream. Corretta ma va scorporata e dichiarata. Manca il caso "literal in non executable memory" (:709-715, translatedAddr==0
  registrato comunque). "with tests that discriminate": nessun test esiste nel repo motore.
- 769d61de: restano compilati i 2 shader RenderFlag_Edge (GPU3D_OpenGL.cpp:136,139), unici consumatori del passaggio morto.
- Upstream melonDS#2751 (nostra): là la guardia Compute non inizializza `HiresCoordinates` e il commento dice "starts at 0" (è -1).

### Cache shader "33/33 rifiutati" — causa radice
Non esiste una cache: lookup e store in OpenGLSupport.cpp:263-279 e :302-313 sono **commentati** (introdotti così da
upstream 043244a5, PR #2041); il Log "from cache was rejected" (:280) è fuori dal blocco e incondizionato; Load/SaveShaderCache
hanno zero chiamanti; 33 = costante (GPU3D_Compute.cpp:94). I 517 ms sono le 33 compilazioni in blocco dentro un frame
(MelonInstance.cpp:343-353; Qt le spalma). Se riattivata: path relativo instradato a UriFileHandler (fallirebbe),
RETRIEVABLE_HINT mai impostato, chiave senza impronta driver, SaveShaderCache incoerente.

### Rischi strutturali preesistenti (non della PR, ma la PR ci passa sopra)
- Stato condiviso main/emu senza sincronizzazione: targetFps/limitFps/fastForward (MelonDSAndroidJNI.cpp:49-52, letti :667,671
  fuori lock), inputMask, `currentConfiguration` shared_ptr riassegnato (:547) letto da syncRTC ogni frame.
- loadStateInternal (MelonDSAndroidJNI.cpp:371-377) senza pausa/lock → RequestFlush concorrenti → UAF su Buffer (SaveManager.cpp:119-124).
- Race audio: onErrorAfterClose da thread Oboe detached vs updateAudioSettings JNI sulle stesse globali (MelonDSAudio.cpp).
  setupAudio (:252) usa solo soundEnabled, updateAudioSettings usa la policy: divergenza.
- Stop emulatore sincrono sul thread UI (EmulatorViewModel.kt:406-409): join + 3 Thread_Wait + 3 scritture SAF; stageSaves allarga.
- Disallineamento shader/texture fino a 8 frame (coda 9) e indefinito in pausa (FrameQueue.cpp:61).
- SCANLINES manca da arrays.xml:49-57 dall'upstream 613cca4f (preesistente).
- ExternalScreenRender/ExternalLayoutRender/ColorRenderer mai istanziati (codice morto).

## 2. Dispositivo (Thor, sola lettura, 12/09 00:19)
Nessun crash/ANR della build PR nella finestra 17:17→00:19. Unico crash nativo: 09/09 16:26 sulla 2.0.1 ufficiale,
SIGTRAP in `melonDS::ARMv4::Execute<(CPUExecuteMode)2>()+268` (EmulatorThread). La build profiling non emette log
applicativo: nessuna evidenza pro/contro cache shader o RTC.

## 3. Ambiente ricostruito sul Mac (12/09)
JDK 21 (brew openjdk@21), SDK ~/Library/Android/sdk (platform 37.2 + 37.0, build-tools 36/37, NDK 28.0.13004108, CMake 3.22.1,
emulator, system image API 35 arm64), AVD `melonds-test`. 164 test Kotlin passano (41 × 4 flavor). assembleDebug OK: 4 APK.
Comando: `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home sh ./gradlew :app:testGitHubNightlyDebugUnitTest`.

## 4. Cosa chiedono gli utenti (issue upstream)
Domanda forte: multi-touch (#1446, manutentore "I'm thinking of reviewing this"), RTC (#1618/#1652/#1604, discussion #1650 cita
il Thor, PR #1664 in revisione), setup BIOS (carico ricorrente #532/#1647/#1646/#44). Senza domanda esplicita: audio avvio, mute,
offset filtri, messaggio BIOS, crash <11, save silenziosi, profiling, edge index, JIT. Compute renderer: solo Adreno per scelta
del manutentore. Righe nere: melonDS#458 ancora aperta. Bisogni scoperti: audio crackling nightly #1583 (26c), Thor crash
libreria grande #1519, RetroAchievements #1611/#1620, WFC #1634, LAN #1493.

## 5. Proposta operativa (da decidere)
**Strategia PR**: chiudere #1672 e riaprire come PR piccole e indipendenti, ciascuna vista funzionare a schermo, con commit
puliti (autore reale, niente trailer di sessione, niente riferimenti a documenti interni, commenti in inglese):
1. Input: multi-pointer + ACTION_CANCEL su tutti gli handler + releaseAll in updateInputs + zero allocazioni + filtro croce
   (decidere) + test veri (androidTest con MotionEvent.obtain). Base: 598b0c32, af4e3141, a30580e3.
2. Audio: i due fix così come sono (+ setupAudio con la stessa policy). Base: 294d2db2, 5ca9741c.
3. Salvataggi: retry (94710907) presentato come tale; opzionale evento verso la UI (EmulatorMessageQueue + ToastEvent) e
   Thread_Free guardato; togliere stageSaves o giustificarla; temp+rename è lavoro a parte.
4. Filtri: tenere il fix per XBR/HQ/Quilez, ripristinare LCD/Scanlines ancorati ai 256×192 nativi; aggiungere scanlines ad
   arrays.xml; non toccare i renderer morti. Meglio ancora: dimensione texture come uniform (niente ricompilazione).
5. BIOS: messaggio console + FirmwareValidation (b968177a, ce2ba962, eff2bdd0) subito; l'assistente guidato **solo dopo**
   coroutine su IO, progress, cancellazione, cleanup dei parziali, rivalidazione, test.
6. RTC: ritirare; eventualmente contribuire a #1664 la decisione "solo avanti con soglia" nel resume nativo.
7. Profiling build type: PR separata o tenerla locale.
**Motore lib#16**: tenerla; correggere il testo (avvertenza SD/mic, timing VRAM, cit. #1548), scorporare la modifica InvalidateByAddr
in un commit dichiarato, aggiungere `if (translatedAddr && …)`, rimuovere i 2 shader Edge; correggere #2751 (HiresCoordinates init, commento).
**Migliorie con fondamento reale**: Log cache shader (1 riga, PR upstream isolata); spalmare le 33 compilazioni su più frame;
cache shader vera (B1-B5) solo se i 517 ms contano; scrittura atomica dei .sav; sincronizzazione stato main/emu; loadState con
attesa pausa come reset.
Ogni intervento va provato nell'AVD (screenshot/misure) e poi sul Thor dall'utente prima di finire in una PR.
